#**Rock, Paper, Scissors**

A simple console-only Rock–Paper–Scissors game

No UI — this project focuses on JavaScript logic, functions, and control flow.

Open `index.html` in your browser, open DevTools → Console, then run `playGame()` to start.  

